name: Caro Windows Crash Diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: [self-hosted, Windows, X64]
    steps:
      - run: echo "Crash diagnostic workflow is being prepared."
