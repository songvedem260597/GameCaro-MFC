name: Caro Windows Smoke

on:
  push:
    branches:
      - ci/caro-windows-smoke-2
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run-game:
    runs-on: [self-hosted, Windows, X64]
    timeout-minutes: 10

    steps:
      - name: Show runner identity
        shell: pwsh
        run: |
          Write-Host "RUNNER_NAME=$env:RUNNER_NAME"
          Write-Host "RUNNER_OS=$env:RUNNER_OS"
          Write-Host "RUNNER_ARCH=$env:RUNNER_ARCH"
          Write-Host "COMPUTERNAME=$env:COMPUTERNAME"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Inspect existing executable
        shell: pwsh
        run: |
          $exe = Join-Path $env:GITHUB_WORKSPACE 'Debug\vebanco.exe'
          if (-not (Test-Path $exe)) {
            throw "Missing executable: $exe"
          }
          Get-Item $exe | Format-List FullName,Length,LastWriteTime
          Get-FileHash $exe -Algorithm SHA256 | Format-List

      - name: Launch Caro smoke test on local PC
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $exe = Join-Path $env:GITHUB_WORKSPACE 'Debug\vebanco.exe'

          try {
            $process = Start-Process -FilePath $exe -WorkingDirectory (Split-Path $exe) -PassThru
          }
          catch {
            Write-Host 'Failed to start the game process.'
            Write-Host $_.Exception.ToString()
            throw
          }

          Write-Host "Started PID $($process.Id) on self-hosted runner $env:RUNNER_NAME"
          Write-Host 'The game should be visible on the interactive Windows desktop for about 15 seconds.'
          Start-Sleep -Seconds 15
          $process.Refresh()

          if ($process.HasExited) {
            throw "Caro exited too early. ExitCode=$($process.ExitCode)"
          }

          Write-Host 'PASS: Caro stayed alive for 15 seconds on the self-hosted Windows runner.'
          Stop-Process -Id $process.Id -Force
